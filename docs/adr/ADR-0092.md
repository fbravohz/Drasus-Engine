### **ADR-0092: Copy-Trading mediante Relé Ciego de Señales (E2E)**

> 🔶 **Enmendado por ADR-0143 (2026-07-06)** — el "Signal Relay" deja de ser un servidor dedicado propio de esta feature. Se reutiliza el **relé cifrado genérico** que ADR-0143 añade a la Cabina de Mando (mismo canal saliente autenticado que ya usa toda instancia para identidad/licencia/telemetría, generalizado a mensajería dirigida zero-knowledge). La disciplina criptográfica de este ADR (cero conocimiento, AES-256-GCM, firma HMAC-SHA256, prohibido puertos entrantes en el Master) se mantiene intacta — solo cambia quién aloja el relé: un componente compartido, no uno construido a propósito para copy-trading.

*   **Decisión:**
    Implementar una topología de distribución de señales de trading de un solo sentido (Master a Copiers) sobre el **relé cifrado genérico de la Cabina de Mando** (ADR-0143) y encriptación simétrica AES-256-GCM de extremo a extremo (E2E).

*   **Objetivo:**
    Para eliminar la vulnerabilidad de seguridad y problemas de ancho de banda del Master inherentes a una arquitectura peer-to-peer (P2P) directa. Conectar múltiples clientes directamente expone la dirección IP del Master a ataques cibernéticos e inestabilidad de red. El relé ciego actúa como un intermediario sin acceso a la lógica ni datos descifrados del trade (Zero-Knowledge), optimizando la distribución de baja latencia a múltiples receptores.

*   **Reglas:**
    - **Cero Conocimiento del Relé:** El relé (componente compartido de la Cabina de Mando, ADR-0143) nunca almacena ni tiene acceso a las claves de encriptación de sesión o el contenido descifrado de las señales.
    - **Cifrado Obligatorio en Origen:** Toda señal debe ser serializada, comprimida, cifrada (AES-256-GCM) y firmada (HMAC-SHA256) antes de ser enviada al relé.
    - **Ejecución y Control de Riesgos Local:** Las órdenes se calculan y ejecutan localmente en la terminal de cada Copier según su capital y límites propios de riesgo, sin control centralizado de los brokers por parte del Master o del Relé.

*   **Implementación:**
    - El Master configura una clave única de sesión y ve el estado de conexión saliente hacia el servidor relé y el volumen de copiers activos en un dashboard de monitoreo.
    - El Copier introduce la clave de acceso criptográfica del Master y sus credenciales locales de broker, observando su latencia de señal (rechazo si >500ms) y su cuenta replicada.
    - El relé es el canal compartido de la Cabina de Mando (ADR-0143): únicamente valida tokens de sesión activos y retransmite bytes binarios asíncronamente hacia el destinatario correcto.

*   **Costo:**
    Carga extra en el Copier para mantener feeds de volatilidad local (ATR) para el escalado de riesgo y latencia marginal añadida por el procesamiento de cifrado/descifrado y el salto intermedio del servidor (<50ms en total).

*   **Trazabilidad:** [`copy-trading-engine.md`](../features/copy-trading-engine.md), [`execute.md`](../modules/execute.md), ADR-0143 (relé cifrado genérico de la Cabina de Mando — enmienda 2026-07-06).

* **Ver también:** ADR-0093 — Marco de seguridad soberana del cual este ADR hereda el estándar criptográfico (AES-256-GCM).

---

