---
name: pricing-foundations-saas
description: "Cimientos de pricing/SaaS de Drasus Engine — DISEÑO COMPLETADO por el Architect (2026-07-03): ADR-0143/0144 + SAD-22 + 9 features + moonshots. Listo para que el Tech-Lead lo implemente ANTES de la auditoría retroactiva."
metadata: 
  node_type: memory
  type: project
  originSessionId: 121318b9-21ba-4d1e-a76b-a3648b955c7b
---

El usuario trabaja EN PARALELO con el Architect el **modelo de pricing de Drasus Engine** (nombre de producto tentativo; podría cambiar — por eso el chrome se centralizó en `kAppName`). Objetivo declarado (2026-07-03): dejar en la base de datos **todas las tablas/columnas** necesarias, más las **features y ADRs**, para soportar **CUALQUIER modelo de negocio/SaaS** que el usuario decida en el futuro, sin migración dolorosa después. Debe cubrir: **tracking de uso, métricas, candados de seguridad, cuotas/límites, y lo necesario para facturación/planes**.

**Why:** filosofía Inundación de Fundaciones (ADR-0020, [[adr-0020-contrato-logico]]): "prefiero tenerlo y no necesitarlo". El pricing toca los cimientos (persistencia, gobernanza, seguridad), así que debe quedar preparado desde la base, no parcheado tarde.

**How to apply:** es **diseño del Architect** — el Tech-Lead NO redacta ADR/SAD/features, solo implementa. Orden del pipeline (PROGRESS.md): STORY-026 (deuda UI) ✅ → **cimientos pricing/SaaS (diseño ✅, implementación pendiente)** → auditoría retroactiva EPIC-0. Va ANTES de la auditoría a propósito, para que ésta contraste también estos fundamentos. Enlaza con [[auditoria-retroactiva-epic0]].

---

## DISEÑO COMPLETADO (2026-07-03) — commits `64e9bbb` (docs) + `0871219` (CLAUDE.md)

**Decisión maestra — ADR-0143:** modelo de **tres planos** (UI local · Ejecución en hardware del usuario: PC o su VPS headless · **Cabina de Mando Central del proveedor**, que NUNCA computa). **Deroga Zero-Telemetry.** Soberanía **condicionada por tier**: gratis = el trabajo del usuario (estrategias/backtests/portafolios/resultados/instrumentos) fluye al proveedor y es suyo por ToS (modelo Google/Facebook); pago al corriente = **supresión de telemetría de trabajo en origen** (privacy-by-design, vendible); pago vencido = emisión reactivada, entorno no se borra. **Secretos (credenciales bróker, IPs live) jamás salen, en ningún tier** (ADR-0093). Enmienda ADR-0016/0030/0102; desambigua ADR-0119 (su "Plano de Control" es del usuario, la Cabina de Mando es del proveedor). Trimodal (ADR-0033): el modo headless = VPS del propio usuario, NO cómputo hosteado por el proveedor.

**ADR-0144 — 9 cimientos** (puerto ADR-0137 + esquema greenfield ADR-0141/0020 ahora; adaptador después): 1 `central-identity`, 2 `licensing-system` (EXTENDIDA, no nueva — derogó su "cero telemetría absoluta"), 3 `plan-tier-quota`, 4 `usage-metering` (libro de nocional USD), 5 `consent-registry`, 6 `enriched-domain-events` (raíz del substrato), 7 `institutional-report-engine`, 8 `third-party-api-gateway`, 9 `data-aggregation`. Facturación = adaptador sobre plan+metering (moonshot `monetization-stripe` existente).

**Clasificación:** Moonshots-producto: `institutional-report-products`, `aggregated-data-feeds`. **Zizaña** (archivada con caveat): `pfof-order-flow` (imposible sin ser ruteador), `capital-allocation-platform` (gestor regulado), `regulatory-signoff-service` (sello exige acreditación). **Descartado** (ni documentado): venta de flujo retail para front-running contra los propios usuarios.

**Gobernanza:** SAD-22 nuevo (Cabina de Mando + substrato), SAD-11 invariante nuevo, SAD-20 enmendado, CLAUDE.md §1 reescrito.

**Doctrina de UI del substrato + aclaración Cabina de Mando (2026-07-04, ratificada por el usuario):** la **Cabina de Mando Central** ES el servidor central del proveedor (autentica/licencia/ingiere telemetría/agrega, NUNCA computa) — NO es el motor de Drasus (ese es el Plano de Ejecución, corre en el hardware del usuario). Implicación clave: lo ÚNICO que depende del sprint lejano de la Cabina es el **adaptador de red** al servidor central; TODA la fontanería (puertos, esquema, lógica del gate, cachés, stubs que sustituyen al servidor) se conecta AQUÍ y AHORA. El día que exista el servidor, se cambia stub→adaptador de red detrás del MISMO puerto (ADR-0144 "puerto ahora, adaptador después"). **Corrección de rumbo:** el TL había sobre-diferido la UI mezclándola con el adaptador central; el SVF y los mocks de galería NO dependen del servidor central. **Doctrina revisada — cada cimiento del substrato entrega:** (1) backend, (2) CLI `verify` (Canal #2), (3) **SVF tab** en el Banco de Verificación (backend real por FFI, para que el usuario lo pruebe), (4) **componentes en galería con mocks** (para entender el comportamiento sin leer código). Solo se difiere: el adaptador de red + el cableado en la UI productiva real. Backend-first: los backends de #3–#9 primero, la tanda de UI (SVF+galería, incl. retroactiva para #1/#2) puede batchearse. Índice de pruebas manual por feature: **`docs/TEST.md`** (nuevo 2026-07-04). Postman/Canal #3 NO iniciado (depende del cimiento #8 gRPC). Ver [[verification-surface-svf]] y [[galeria-componentes-estado]].

**Revisión de gaps del substrato (2026-07-05, Architect):** el usuario trajo un análisis de 8 gaps para confirmar. Verificado contra el filesystem: entre 07-03 y 07-05 el TL implementó cimientos #1-#5 (STORY-027..031, QA APTO), añadió ADR-0145 + `verified-account-registry` (cimiento #10) y `TEST.md`. Clasificación: **Intencionales (NO huecos — "puerto ahora, adaptador después"):** Gap 1 sync local-cloud (adaptador de red), Gap 2 servidor gRPC central (ADR-0142 exige Spike), Gap 4 Consumption Dashboard (UI productiva diferida; SVF+galería son el interino por la doctrina 07-04). **Integrados como diseño (edits 07-05):** Gap 7 FX → nota en `usage-metering` (nocional no-USD reutiliza la valoración del motor, tipo de cambio congelado con la operación, sin proveedor FX aparte) + autoridad de reconciliación (Cabina de Mando autoritativa, local obedece); Gap 8 degradación mid-cycle → invariante FIJO en `licensing-system`: `ExecutionGate` NUNCA bloquea reduce-only (jamás atrapa capital), contador de nocional no se reinicia por downgrade; Gap 6 transferencia entre máquinas → `licensing-system` param `TRANSFER_COOLDOWN` + comportamiento self-service de desvinculación; Gap 3 grafo de colisión anti-abuso → nota en `central-identity` (local solo emite huella+señal; correlación cross-cuenta es responsabilidad diferida del servidor central; ML EPIC-9+). **Nuevo moonshot:** `encrypted-local-backup` (Gap 5) — respaldo de la DB soberana cifrado client-side (AES-256-GCM), el proveedor guarda un blob que no puede leer; perk del tier de pago coherente con ADR-0143/0093; moonshot (adaptador de durabilidad) no cimiento (no necesita puerto vivo ahora). **Anclaje de lo diferido (07-05):** el ROADMAP (bloque Cimientos de Monetización) ahora tiene una tabla "Diferido — anclaje y disparadores" que da fase/disparador a cada pieza aplazada — Cabina de Mando (servidor Rust aparte: Auth/Billing/Metering/Device+colisión/Backup) → disparador = primer cobro real tras EPIC-5, precondición Spike gRPC ADR-0142; adaptadores de red sync → con la Cabina, reconciliación servidor-autoritativo; panel de consumo productivo → EPIC-8 (interino en DEBT-005). Sistema de 3 registros: **ROADMAP** (fases, Architect) + **DEBT.md** (deuda granular, TL, regla "si no está aquí no está rastreada") + banners "Pendiente:" por feature. El nº de EPIC del servidor central es punto abierto de secuenciación (se decide al acercarse EPIC-5). Cambios sin commitear al cierre de esta revisión.

## IMPLEMENTACIÓN TL — SUBSTRATO 7/10 (2026-07-06)

El Tech-Lead lleva **#1–#8 cerrados** (#1–#7 commiteados; #8 cerrado, commit pendiente de autorización) — puerto + esquema greenfield; adaptador de red diferido a la Cabina. Ciclo por cimiento: Gate de Coherencia → Orden `docs/execution/STORY-XXX.md` → Rust-Engineer (Sonnet) → auditoría TL independiente (reproducir, nunca confiar en el reporte) → **QA gate obligatorio con mutación (APTO o no se cierra)** → sellar feature + PROGRESS + DEBT → proponer commits por tipo → **el usuario autoriza cada commit** (ver [[feedback-commit-cadence]]).

- #1 `central-identity`, #2 `licensing-system` (con `ExecutionGate.suppress_work_telemetry`), #3 `plan-tier-quota`, #4 `usage-metering`, #5 `consent-registry` (STORY-031), #6 `enriched-domain-events` (STORY-033, 8 variantes incl. los 3 de ADR-0145: flujo de capital/snapshot de cuenta/orden reforzada; consume el `ExecutionGate` real), #7 `institutional-report-engine` (STORY-034, firma reproducible ≠ audit_hash). Todos montos `i64` ×10⁸, serialización canónica `BTreeMap`+SHA-256, cero `f64`.
- #8 `third-party-api-gateway` (STORY-035, QA APTO 2026-07-06): dos tablas (`api_credentials` MUTABLE row_version + `api_usage_records` APPEND-ONLY atómica), auth por hash SHA-256 (revocación-gana), rate-limit borde exacto, consume el `consent_out` real de #5. Servidor gRPC público = Canal #3 diferido. Faltan #9 `data-aggregation` y #10 `verified-account-registry`.
- **Regla nacida esta sesión (causa raíz DEBT-001):** todo ledger append-only nace atómico — `BEGIN IMMEDIATE` + `busy_timeout` + reintento acotado + `WriteContention` tipado; prueba de 2 escritores obligatoria (DB en archivo, NO `:memory:`). Ya en skills `rust-engineer`/`qa-engineer`. Ver [[debt-registry-y-atomicidad]].
- **DEBT.md es el registro canónico de deuda** (análogo a TEST.md); esta sesión se saldaron DEBT-001/007 (STORY-032 de endurecimiento) y se abrieron DEBT-008/009/010 (#6 sin fan-out al bus; placeholders de tipos del guantelete en #7; render Tera diferido). Sistema de 3 registros: ROADMAP (fases, Architect) + DEBT.md (granular, TL) + banners por feature.
- **Cabina de Mando = repo hermano** `../drasus-portal/`, ver [[drasus-portal-cabina-de-mando]].

**Follow-ups — RESUELTOS (2026-07-03):** (1) ✅ los 10 tipos del substrato (`AccountIdentity`, `ExecutionGate`, `PlanLimits`, `UsageRecord`, `ConsentVerdict`, `EnrichedDomainEvent`, `InstitutionalReport`, `ThirdPartyRequest/Response`, `AggregatedIndex`) registrados como `textLabel` en el catálogo de ADR-0137 (enmienda 2026-07-03). (2) ✅ nota cruzada `third-party-api-gateway` (API pública de terceros) ↔ `saas-gateway`/`saas-cloud-engine` (ingreso del cliente headless del propio usuario) añadida en ambos docs. **Diseño 100% cerrado, sin pendientes documentales.** Enlaza con [[arquitectura-visual-canvas]] (puertos tipados ADR-0137).
